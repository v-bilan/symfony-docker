server {
    listen 80;
    server_name localhost;

    # Коренева директорія вашого веб-сайту
    root /usr/share/nginx/html;
    index index.html index.htm;

    # Налаштування для обробки запитів
    location / {
        try_files $uri $uri/ =404;
    }
}